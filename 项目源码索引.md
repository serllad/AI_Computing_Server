# BISHENG 项目源码索引

## 项目概述

**BISHENG（毕昇）** 是一个面向企业的 LLM 应用 DevOps 平台，专注于企业级智能应用开发与运维。项目采用**前后端分离 + 异步任务处理**的分层架构，支持复杂的企业应用场景。

### 核心特性
- **Linsight 通用智能体**：通过 AGL（Agent Guidance Language）框架实现专家级任务处理
- **BISHENG Workflow**：独立的工作流编排框架，支持循环、并行、批处理、条件逻辑
- **企业级功能**：安全审查、RBAC、用户组管理、流量控制、SSO/LDAP
- **高精度文档解析**：自研的文档解析模型，支持印刷体、手写体、表格识别

---

## 技术栈

### 后端
| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.11+ | 运行时 |
| FastAPI | - | Web 框架 |
| uvicorn | - | ASGI 服务器 |
| SQLModel / SQLAlchemy | - | ORM |
| Celery | 5.x | 异步任务队列 |
| LangGraph | 1.2+ | 工作流执行引擎 |
| LangChain | 1.3+ | LLM 编排框架 |
| Pydantic | v2 | 数据校验与序列化 |
| Loguru | - | 结构化日志 |
| uv | - | 依赖管理 |

### 前端
| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.x | UI 框架 |
| TypeScript | - | 类型安全 |
| Vite (SWC) | - | 构建工具 |
| Zustand | - | 状态管理（平台端） |
| Recoil | - | 状态管理（客户端） |
| Radix UI | - | 无障碍组件库 |
| Tailwind CSS | - | 原子化样式 |
| @xyflow/react | - | 工作流画布 |
| Axios | - | HTTP 客户端 |
| i18next | - | 国际化（中/英/日） |

### 基础设施
| 服务 | 版本 | 端口 | 用途 |
|------|------|------|------|
| MySQL | 8.0 | 3306 | 关系型数据存储 |
| Redis | 7.0 | 6379 | 缓存、Celery Broker、会话存储 |
| Milvus | 2.5.10 | 19530 | 向量存储（RAG 稠密向量检索） |
| Elasticsearch | 8.12 | 9200 | 全文检索、遥测数据存储 |
| MinIO | - | 9000 | S3 兼容对象存储 |

---

## 项目结构

```
AI_Computing_Server-main/
├── src/                          # 源代码根目录
│   ├── backend/                  # 后端服务
│   │   ├── bisheng/             # Python 包主目录
│   │   │   ├── api/             # API 层（路由定义）
│   │   │   │   ├── router.py    # 全局路由注册
│   │   │   │   └── v1/          # v1 路由端点
│   │   │   ├── common/          # 公共模块（错误码、依赖、中间件）
│   │   │   ├── core/            # 核心模块（配置、数据库、上下文）
│   │   │   ├── database/        # 数据库模型层
│   │   │   ├── worker/          # Celery 异步任务
│   │   │   ├── workflow/        # 工作流引擎
│   │   │   │   ├── graph/       # LangGraph 图引擎
│   │   │   │   ├── nodes/       # 工作流节点
│   │   │   │   ├── edges/       # 工作流边
│   │   │   │   └── common/      # 工作流公共定义
│   │   │   ├── knowledge/       # 知识库/RAG 模块
│   │   │   │   ├── api/         # 知识库 API
│   │   │   │   ├── domain/      # 领域层（服务、模型、仓储）
│   │   │   │   └── rag/         # RAG 流水线实现
│   │   │   ├── linsight/        # Linsight 智能体
│   │   │   │   ├── api/         # Linsight API
│   │   │   │   ├── domain/      # 领域层
│   │   │   │   └── worker.py    # Linsight Worker 进程
│   │   │   ├── llm/             # LLM 模型管理
│   │   │   ├── permission/      # 权限管理（ReBAC/OpenFGA）
│   │   │   ├── tenant/          # 多租户管理
│   │   │   ├── approval/        # 审批中心
│   │   │   ├── chat_session/    # 会话管理
│   │   │   ├── channel/         # 渠道集成
│   │   │   ├── message/         # 消息收件箱
│   │   │   ├── evaluation/      # 模型评测
│   │   │   ├── finetune/        # 模型微调
│   │   │   ├── user/            # 用户管理
│   │   │   ├── role/            # 角色管理
│   │   │   ├── user_group/      # 用户组
│   │   │   ├── department/      # 部门管理
│   │   │   ├── admin/           # 管理后台
│   │   │   ├── brand/           # 品牌定制
│   │   │   ├── sensitive_word/  # 敏感词管理
│   │   │   ├── share_link/      # 分享链接
│   │   │   ├── telemetry/       # 遥测统计
│   │   │   ├── telemetry_search/# 遥测数据检索
│   │   │   ├── citation/        # 引用管理
│   │   │   ├── tool/            # 工具集成
│   │   │   ├── mcp_manage/      # MCP 管理
│   │   │   ├── open_endpoints/  # 开放 API（v2 RPC）
│   │   │   ├── org_sync/        # 组织同步
│   │   │   ├── sso_sync/        # SSO 同步
│   │   │   ├── workstation/     # 工作台
│   │   │   ├── main.py          # FastAPI 应用入口
│   │   │   └── config.yaml      # 配置文件
│   │   ├── bisheng_langchain/   # LangChain 扩展
│   │   ├── test/                # 测试目录
│   │   ├── scripts/             # 脚本工具
│   │   ├── pyproject.toml       # Python 项目配置
│   │   └── Dockerfile           # Docker 构建文件
│   │
│   └── frontend/                # 前端应用
│       ├── platform/            # 管理平台 UI
│       │   └── src/
│       │       ├── pages/       # 页面组件
│       │       │   ├── BuildPage/      # 工作流构建
│       │       │   ├── KnowledgePage/  # 知识库管理
│       │       │   ├── ModelPage/      # 模型管理
│       │       │   ├── ChatAppPage/    # 应用管理
│       │       │   ├── EvaluationPage/ # 模型评测
│       │       │   ├── Dashboard/      # 仪表盘
│       │       │   ├── SystemPage/     # 系统设置
│       │       │   ├── TenantPage/     # 租户管理
│       │       │   ├── ApprovalPage/   # 审批中心
│       │       │   └── ...
│       │       ├── components/  # 公共组件
│       │       ├── store/       # Zustand 状态管理
│       │       ├── controllers/ # API 控制器
│       │       ├── hooks/       # 自定义 Hooks
│       │       ├── contexts/    # React Context
│       │       ├── routes/      # 路由配置
│       │       └── utils/       # 工具函数
│       │
│       ├── client/              # 用户聊天 UI
│       │   └── src/
│       │       ├── pages/       # 页面组件
│       │       ├── components/  # 公共组件
│       │       ├── store/       # Recoil 状态管理
│       │       ├── api/         # API 调用
│       │       └── ...
│       │
│       ├── packages/            # 共享包
│       │   ├── ui/              # 共享 UI 组件库
│       │   ├── locales/         # 国际化资源
│       │   └── file-viewers/    # 文件查看器
│       │
│       └── package.json         # 前端项目配置
│
├── docs/                        # 文档目录
│   ├── architecture/            # 架构文档
│   │   ├── 01-architecture-overview.md  # 系统架构总览
│   │   ├── 02-backend-modules.md        # 后端模块详解
│   │   ├── 03-workflow-engine.md        # 工作流引擎
│   │   ├── 04-knowledge-rag.md          # 知识库/RAG
│   │   ├── 05-linsight-agent.md         # Linsight 智能体
│   │   ├── 06-frontend-architecture.md  # 前端架构
│   │   ├── 07-data-models.md            # 数据模型
│   │   ├── 08-deployment.md             # 部署指南
│   │   ├── 09-development-guide.md      # 开发指南
│   │   ├── 10-permission-rbac.md        # 权限体系
│   │   ├── 11-gateway.md                # 网关
│   │   └── 12-multi-tenant.md           # 多租户
│   ├── api/                     # API 文档
│   ├── product/                 # 产品文档
│   └── constitution.md          # 架构宪法（不可违反的规则）
│
├── docker/                      # Docker 部署配置
│   ├── docker-compose.yml       # 主要编排文件
│   ├── bisheng/                 # BISHENG 容器配置
│   ├── mysql/                   # MySQL 配置
│   ├── nginx/                   # Nginx 配置
│   ├── redis/                   # Redis 配置
│   └── office/                  # OnlyOffice 配置
│
├── scripts/                     # 脚本工具
│   ├── arch-guard.sh            # 架构守护脚本
│   └── ...
│
├── features/                    # SDD 功能规格
├── tools/                       # 工具目录
│
├── AGENTS.md                    # AI 代理开发指南
├── CLAUDE.md                    # Claude 开发指南
├── README.md                    # 项目说明
└── docker-compose.yml           # 根目录编排文件
```

---

## 核心模块详解

### 1. 工作流引擎（Workflow）

**路径**: `src/backend/bisheng/workflow/`

基于 LangGraph 的 DAG 工作流执行引擎，支持：
- 循环、并行、批处理、条件逻辑
- 人机交互（Human-in-the-loop）
- 多类型输入/输出
- 报告生成、内容审核等复杂场景

**关键文件**:
- `graph/graph_engine.py` - 图执行引擎
- `graph/graph_state.py` - 图状态管理
- `graph/workflow.py` - 工作流定义
- `nodes/node_manage.py` - 节点管理器
- `common/node.py` - 节点类型定义（NodeType 枚举）

**扩展节点**: 在 `nodes/<name>/` 下创建节点类，继承 `BaseNode`，注册到 `NODE_CLASS_MAP`。

### 2. 知识库/RAG

**路径**: `src/backend/bisheng/knowledge/`

知识库管理与 RAG（检索增强生成）实现：
- **Load → Transform → Ingest** 流水线
- **Milvus（稠密向量）+ ES（稀疏关键词）** 双写
- 异步处理：通过 `knowledge_celery` 队列

**关键文件**:
- `rag/pipeline/base.py` - RAG 流水线基类
- `domain/services/` - 领域服务
- `api/endpoints/` - API 端点

### 3. Linsight 智能体

**路径**: `src/backend/bisheng/linsight/`

独立运行的智能体推理进程：
- 通过 Redis 队列接收任务
- 支持 AGL（Agent Guidance Language）框架
- 专家级任务处理能力

**关键文件**:
- `worker.py` - Linsight Worker 进程入口
- `domain/task_exec.py` - 任务执行逻辑
- `api/router.py` - API 路由

### 4. 权限管理（Permission）

**路径**: `src/backend/bisheng/permission/`

基于 ReBAC（Relationship-Based Access Control）的权限体系：
- 使用 OpenFGA 实现细粒度权限控制
- 支持资源级权限管理
- 与多租户深度集成

**关键文件**:
- `core/openfga/manager.py` - OpenFGA 客户端
- `api/router.py` - 权限 API
- `domain/services/` - 权限服务

### 5. 多租户（Tenant）

**路径**: `src/backend/bisheng/tenant/`

企业级多租户支持：
- 数据隔离（自动注入 tenant_id）
- ContextVar 实现租户上下文传递
- 支持租户级别的配置管理

**关键文件**:
- `core/context/tenant.py` - 租户上下文管理
- `database/tenant_filter.py` - 租户过滤器

### 6. Celery 异步任务

**路径**: `src/backend/bisheng/worker/`

异步任务处理架构：
- **knowledge_celery**: 文档解析、Embedding 生成、向量写入（20 线程）
- **workflow_celery**: 工作流 DAG 执行（100 线程）
- **celery（默认）**: 遥测统计收集（100 线程）
- **Beat**: 定时任务调度

**关键文件**:
- `main.py` - Celery 应用入口
- `knowledge/` - 知识库任务
- `workflow/` - 工作流任务
- `approval/` - 审批任务

---

## API 路由架构

### v1 路由（`/api/v1`）- 面向前端

29 个子路由模块，提供完整的 CRUD 操作界面：

| 路由模块 | 职责 |
|---------|------|
| `chat_router` | 对话管理 |
| `knowledge_router` | 知识库 CRUD |
| `workflow_router` | 工作流管理 |
| `assistant_router` | AI 助手管理 |
| `llm_router` | 模型供应商管理 |
| `user_router` | 用户认证与管理 |
| `group_router` | 用户组/RBAC |
| `evaluation_router` | 模型评测 |
| `finetune_router` | 模型微调 |
| `linsight_router` | 灵思智能体 |
| `session_router` | 会话管理 |
| `channel_router` | 渠道集成 |
| `approval_router` | 审批中心 |
| `tenant_router` | 租户管理 |
| `permission_router` | 权限管理 |
| ... | ... |

### v2 路由（`/api/v2`）- 面向外部集成

6 个 RPC 风格路由：

| 路由模块 | 职责 |
|---------|------|
| `knowledge_router_rpc` | 知识库操作 |
| `filelib_router_rpc` | 文件库操作 |
| `chat_router_rpc` | 对话接口 |
| `assistant_router_rpc` | 助手调用 |
| `workflow_router_rpc` | 工作流触发 |
| `llm_router_rpc` | LLM 调用（含 OpenAI 兼容接口） |

---

## 开发指南

### 后端开发

**依赖管理**: 使用 `uv` 管理 Python 依赖
```bash
cd src/backend
uv sync --frozen
```

**启动 API 服务**:
```bash
export config=config.yaml
uv run uvicorn bisheng.main:app --host 0.0.0.0 --port 7860 --workers 1
```

**启动 Celery Worker**:
```bash
uv run celery -A bisheng.worker.main worker -l info -c 20 -P threads -Q knowledge_celery
uv run celery -A bisheng.worker.main worker -l info -c 100 -P threads -Q workflow_celery
uv run celery -A bisheng.worker.main beat -l info
```

**启动 Linsight Worker**:
```bash
uv run python bisheng/linsight/worker.py --worker_num 4 --max_concurrency 5
```

**数据库迁移**:
```bash
uv run alembic upgrade head
uv run alembic revision --autogenerate -m "msg"
```

**测试**:
```bash
uv run pytest test/                    # 全部测试
uv run pytest test/<module>/           # 模块测试
uv run pytest test/ -k "keyword"       # 关键词过滤
```

**代码格式化**:
```bash
uv run ruff format <file_or_dir>
uv run ruff check --fix <file_or_dir>
```

### 前端开发

**安装依赖**:
```bash
cd src/frontend
pnpm install
```

**启动开发服务器**:
```bash
pnpm dev:platform    # 管理平台 :3001
pnpm dev:client      # 用户聊天 :4001
```

**构建**:
```bash
pnpm build:platform  # 构建管理平台
pnpm build:client    # 构建用户聊天
pnpm build           # 构建全部
```

**代码检查**:
```bash
pnpm lint            # ESLint 检查
pnpm typecheck       # TypeScript 类型检查
```

### Docker 部署

**构建镜像**:
```bash
# 后端
cd src/backend
docker build -t dataelement/bisheng-backend:v3.0.0-beta1 .

# 前端
cd ../frontend
docker build -t dataelement/bisheng-frontend:v3.0.0-beta1 .
```

**启动服务**:
```bash
cd docker
docker compose -f docker-compose.yml -p bisheng up -d
```

**访问地址**:
- 管理平台: http://IP:3001
- 用户聊天: http://IP:4001/workspace
- 后端 API: http://IP:7860

---

## 架构约束（不可违反）

详见 `docs/constitution.md`，核心约束：

1. **C1 - DDD 分层**: Router → Endpoint → Service → Repository → ORM
2. **C2 - 双数据库支持**: MySQL + DM8（达梦）
3. **C3 - 多租户隔离**: 所有查询自动注入 tenant_id
4. **C4 - 权限检查**: 所有权限通过 OpenFGA 验证
5. **C5 - 错误码规范**: MMMEE 格式（模块号 + 错误码）
6. **C6 - 安全规范**: 敏感数据加密、输入验证
7. **C7 - 前端规范**: Store 不得直接调用 HTTP
8. **C8 - 部署规范**: 单机部署不能掩盖多节点问题

---

## 关键枚举值

### FlowType（数据库/models/flow.py）
- ASSISTANT = 5（助手）
- WORKFLOW = 10（工作流）
- WORKSTATION = 15（工作台）
- LINSIGHT = 20（灵思智能体）
- CHANNEL_ARTICLE = 25（渠道文章）
- KNOLEDGE_SPACE = 30（知识空间，注意拼写错误）

### 文件状态
- WAITING(5) → PROCESSING(1) → SUCCESS(2)/FAILED(3)/TIMEOUT(6)

---

## 存储引擎

| 引擎 | 用途 |
|------|------|
| MySQL | 关系型数据（用户、会话、模型配置等） |
| Redis | 缓存、Celery Broker、会话存储、Linsight 状态 |
| Milvus | 向量存储（RAG 稠密向量检索） |
| Elasticsearch | 全文检索（BM25）、遥测数据存储 |
| MinIO | S3 兼容对象存储（文档、图片、模型文件） |
| OpenFGA | ReBAC 权限数据 |

---

## 配置管理

配置加载优先级：`YAML → env (BS_*) → DB (initdb_config) → Redis (100s TTL)`

**主要配置文件**: `src/backend/bisheng/initdb_config.yaml`

**环境变量**: 以 `BS_` 为前缀，如 `BS_MYSQL_URL`、`BS_REDIS_URL`

---

## 相关文档

- **架构总览**: `docs/architecture/01-architecture-overview.md`
- **后端模块**: `docs/architecture/02-backend-modules.md`
- **工作流引擎**: `docs/architecture/03-workflow-engine.md`
- **知识库/RAG**: `docs/architecture/04-knowledge-rag.md`
- **Linsight 智能体**: `docs/architecture/05-linsight-agent.md`
- **前端架构**: `docs/architecture/06-frontend-architecture.md`
- **数据模型**: `docs/architecture/07-data-models.md`
- **部署指南**: `docs/architecture/08-deployment.md`
- **开发指南**: `docs/architecture/09-development-guide.md`
- **权限体系**: `docs/architecture/10-permission-rbac.md`
- **多租户**: `docs/architecture/12-multi-tenant.md`
- **架构宪法**: `docs/constitution.md`

---

## 常见问题

1. **租户过滤只拦截 SELECT**: `do_orm_execute` 监听器对非 SELECT 操作返回早退，需要手动添加 tenant_id 条件
2. **ruff 会删除未使用的导入**: 确保导入和使用在同一个编辑中完成
3. **Celery Beat × 多租户**: Beat 任务需要遍历所有活跃租户，调用 `set_current_tenant_id()`
4. **DB 配置变更延迟**: 配置缓存在 Redis 中，TTL 为 100 秒
5. **单机部署隐藏多节点问题**: 文件共享在单机上正常，多节点部署时需要检查跨进程文件访问
